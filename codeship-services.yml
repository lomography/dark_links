dark-links:
  build:
    image: lomography/dark-links
    dockerfile: Dockerfile
    args:
      RUBY_VERSION: '2.7.2'
      BUNDLER_VERSION: '2.1.4'
  environment:
    RAILS_ENV: test
  cached: true
